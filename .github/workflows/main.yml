name: Build Mission 26 APK
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Build APK with Buildozer
        uses: ArtemSerebrennkov/buildozer-action@v1
        with:
          python-version: 3.8
